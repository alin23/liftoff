@import 'rupture'
@import 'nib'

rupture.mobile-cutoff = 450px
rupture.scale = 0 450px 600px 800px 1050px 1800px

$red = #EA6065
$yellow = #EAA960
$green = #8EBA40

$mono = Menlo, Consolas, Monaco, Liberation Mono, Lucida Console, monospace
$sans = avenir next, avenir, -apple-system, BlinkMacSystemFont, helvetica neue, helvetica, Ubuntu, roboto, noto, segoe ui, arial, sans-serif
$serif = Iowan Old Style, Apple Garamond, Baskerville, Times New Roman, Droid Serif, Times, Source Serif Pro, serif, Apple Color Emoji, Segoe UI Emoji, Segoe UI Symbol
$leftMargin = 1.5rem
$horizontalMargin = 1.5rem
$productSize = 200px
$tabletProductSize = 150px
$mobileProductSize = 120px
$smallMobileProductSize = 100px

.field-name
  display: none
  visibility: hidden

html
  color: #141414
  background-color: #fafafa

body
  width: "calc(100vw - (%s + %s))" % ($leftMargin $horizontalMargin)
  max-width: "calc(100vw - (%s + %s))" % ($leftMargin $horizontalMargin)
  margin 0
  margin-left $leftMargin
  margin-top 2rem

  &::before
    display: none
    background-image: none

  +mobile(orientation: portrait)
    margin-left: auto
    margin-right: auto

.row-page
  .field, .header, .nav-button
    margin-left 1.5rem
  .row
    min-height 80vh

  .Produs
    font-size 3rem
    line-height 3rem
    font-weight bold
    font-family $sans

    +mobile(orientation: portrait)
      text-align left

  .Pret
    +mobile(orientation: portrait)
      text-align left

  .Imagine
    margin 0

    &.indisponibil
      filter: saturate(0.3)

  img
    margin: 0
    margin-top 2rem
    width 300px
    height: 300px
    object-fit: cover


.Imagine
  filter: brightness(0.9)
  &.indisponibil
    filter: saturate(0.4) brightness(0.5)

.index-page
  display: flex
  justify-content: flex-start
  align-items: flex-start
  flex-wrap: wrap

  +mobile(orientation: portrait)
    justify-content: space-between
  +tablet(orientation: portrait)
    justify-content: space-between


.row-link
  margin-bottom: 2rem
  margin-left: $horizontalMargin
  margin-right: $horizontalMargin

  +mobile(orientation: portrait)
    margin-bottom: 3rem
    margin-left: 0
    margin-right: 0

  &:last-of-type .row
    margin-bottom: 0

  .row::after
    display: none

  ul
    margin: 0

  img
    width: $productSize
    height: $productSize
    box-shadow: 0 2.8px 2.2px rgba(0, 0, 0, 0.034),
          0 6.7px 5.3px rgba(0, 0, 0, 0.048),
          0 12.5px 10px rgba(0, 0, 0, 0.06)
    border-radius: 8px
    object-fit: cover
    margin: 0

    +to-width(1)
      width: $smallMobileProductSize
      height: $smallMobileProductSize

    +mobile(orientation: portrait)
      width: $mobileProductSize
      height: $mobileProductSize

    +tablet(orientation: portrait)
      width: $tabletProductSize
      height: $tabletProductSize

  .row
    +mobile(orientation: portrait)
      margin-left 0.6rem
      margin-right 0.6rem

.Pret
  // font-family: $mono
  font-weight: bold
  font-size: 1rem
  margin-top: 0

  +mobile(orientation: portrait)
    text-align center

  +above(2)
    display inline

    &.Maxim
      margin-left 0.7ex

  &.Minim
    color: $yellow

  &.Maxim
    color: $green

  &.indisponibil
    color: $red

  & span
    color: slategray

.Produs
  font-weight 500
  font-size 1.1rem
  +mobile(orientation: portrait)
    text-align center

.Nota
  font-weight 500
  font-size 0.9rem
  color #626262
  background aliceblue

  +mobile(orientation: portrait)
    text-align center
  p
    margin: 0
    max-width: $productSize
    padding 3px 6px
    box-sizing: border-box
    border-radius 3px

    +to-width(1)
      width: $smallMobileProductSize
    +mobile(orientation: portrait)
      max-width: $mobileProductSize
    +tablet(orientation: portrait)
      max-width: $tabletProductSize


.header
  margin-top: 0

  h1
    font-weight: 800
    margin-top: 0

  & + h1
    margin-bottom: 4rem
    font-size: 1.5rem
    font-weight: bold

  &, & + h1
    font-family: $sans
    font-weight: bold
    flex-grow: 1
    flex-basis: 100%
    flex-shrink: 0

